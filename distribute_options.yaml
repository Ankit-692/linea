output: dist/
releases:
  - name: Ankit
    jobs:
      - name: release-linux-deb
        package:
          platform: linux
          target: deb
      - name: release-linux-appimage
        package:
          platform: linux
          target: appimage

# output: dist/

# env:
#   APP_NAME: "linea"

# releases:
#   - name: release
#     jobs:
#       # Build .deb Package
#       - name: deb-package
#         package:
#           platform: linux
#           target: deb
#           build_args:
#             release: true

#       # Build AppImage
#       - name: appimage-package
#         package:
#           platform: linux
#           target: appimage
#           build_args:
#             release: true